<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="TabItemLightGrayBorderBrush" Color="#FF333333" />

    <!-- TabViewのタブアイテム用モダンスタイル -->
    <Style x:Key="ModernTabViewItemStyle" TargetType="TabViewItem">
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="MinHeight" Value="34" />
        <Setter Property="Padding" Value="14,0,10,0" />
        <Setter Property="Background" Value="{ThemeResource ControlFillColorTertiaryBrush}" />
        <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabViewItem">
                    <Grid x:Name="LayoutRoot" Margin="-1,4,0,0">

                        <Border x:Name="TabContainer"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource TabItemLightGrayBorderBrush}"
                                BorderThickness="1,1,1,0"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Padding="2,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <ContentPresenter x:Name="IconBox"
                                                  Content="{TemplateBinding IconSource}"
                                                  Margin="0,0,8,0"
                                                  VerticalAlignment="Center"
                                                  Visibility="Collapsed" />

                                <ContentPresenter x:Name="ContentPresenter"
                                                  Grid.Column="1"
                                                  Content="{TemplateBinding Header}"
                                                  ContentTemplate="{TemplateBinding HeaderTemplate}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}" />

                                <Button x:Name="CloseButton"
                                        Grid.Column="2"
                                        Content="&#xE711;"
                                        FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                        FontSize="10"
                                        Width="24" Height="24"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Margin="4,0,0,0"
                                        Padding="0"
                                        CornerRadius="12"
                                        Opacity="0"
                                        VerticalAlignment="Center"
                                        ToolTipService.ToolTip="タブを閉じる" />
                            </Grid>
                        </Border>

                        <!-- 選択タブ下部のアクセントライン -->
                        <Border x:Name="AccentLine"
                                VerticalAlignment="Bottom"
                                HorizontalAlignment="Center"
                                Height="2"
                                Width="0"
                                CornerRadius="1"
                                Background="{ThemeResource AccentFillColorDefaultBrush}" />

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />

                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="TabContainer.Background" Value="{ThemeResource SubtleFillColorSecondaryBrush}" />
                                        <Setter Target="CloseButton.Opacity" Value="0.7" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="TabContainer.Background" Value="{ThemeResource SubtleFillColorTertiaryBrush}" />
                                        <Setter Target="CloseButton.Opacity" Value="0.7" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="Selected">
                                    <VisualState.Setters>
                                        <Setter Target="TabContainer.Background" Value="{ThemeResource ControlFillColorSecondaryBrush}" />
                                        <Setter Target="AccentLine.Width" Value="32" />
                                        <Setter Target="CloseButton.Opacity" Value="0.8" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="PointerOverSelected">
                                    <VisualState.Setters>
                                        <Setter Target="TabContainer.Background" Value="{ThemeResource ControlFillColorSecondaryBrush}" />
                                        <Setter Target="AccentLine.Width" Value="32" />
                                        <Setter Target="CloseButton.Opacity" Value="1" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="PressedSelected">
                                    <VisualState.Setters>
                                        <Setter Target="TabContainer.Background" Value="{ThemeResource ControlFillColorSecondaryBrush}" />
                                        <Setter Target="AccentLine.Width" Value="24" />
                                        <Setter Target="CloseButton.Opacity" Value="1" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TabView自体のスタイル -->
    <Style x:Key="ModernTabViewStyle" TargetType="TabView">
        <Setter Property="Background" Value="Transparent" />
    </Style>
</ResourceDictionary>
