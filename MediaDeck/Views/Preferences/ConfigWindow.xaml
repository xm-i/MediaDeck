<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MediaDeck.Views.Preferences.ConfigWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MediaDeck.Views.Preferences"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
	xmlns:config="using:MediaDeck.ViewModels.Preferences.Config"
    mc:Ignorable="d"
	Title="Configuration">
	<Grid RowDefinitions="*,Auto" ColumnDefinitions="200,*">
		<StackPanel Grid.Column="0" Padding="12" Spacing="8">
			<TextBlock Text="Categories" FontWeight="SemiBold"/>
			<ListView x:Name="CategoryList" ItemsSource="{x:Bind ViewModel.ConfigPageViewModels}" SelectedItem="{x:Bind ViewModel.SelectedPageViewModel.Value, Mode=TwoWay}">
				<ListView.ItemTemplate>
					<DataTemplate x:DataType="config:IConfigPageViewModel">
						<TextBlock Text="{Binding PageName}"/>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</StackPanel>
		<Frame x:Name="ContentFrame" Grid.Column="1"/>
		<Grid Grid.Row="1" Grid.ColumnSpan="2">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>
			<Button Grid.Row="1" Content="Save" Command="{x:Bind ViewModel.SaveCommand}"/>
		</Grid>
	</Grid>
</Window>
