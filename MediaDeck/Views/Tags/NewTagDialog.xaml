<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
	x:Class="MediaDeck.Views.Tags.NewTagDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:MediaDeck.Views.Tags"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:db="using:MediaDeck.Database.Tables"
	mc:Ignorable="d"
	Title="Create New Tag"
	PrimaryButtonText="Create"
	CloseButtonText="Cancel"
	DefaultButton="Primary"
	MinWidth="400">
	<StackPanel Spacing="16" Padding="16">
		<StackPanel Spacing="6">
			<TextBlock Text="Tag Name" FontWeight="SemiBold" />
			<TextBox Text="{x:Bind ViewModel.TagName.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Enter tag name" />
		</StackPanel>
		<StackPanel Spacing="6">
			<TextBlock Text="Category" FontWeight="SemiBold" />
			<ComboBox HorizontalAlignment="Stretch" ItemsSource="{x:Bind ViewModel.TagCategories, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedCategory.Value, Mode=TwoWay}">
				<ComboBox.ItemTemplate>
					<DataTemplate x:DataType="db:TagCategory">
						<TextBlock Text="{x:Bind TagCategoryName}" />
					</DataTemplate>
				</ComboBox.ItemTemplate>
			</ComboBox>
		</StackPanel>
		<StackPanel Spacing="6">
			<TextBlock Text="Description (Optional)" FontWeight="SemiBold" />
			<TextBox Text="{x:Bind ViewModel.Detail.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Enter description" Height="80" AcceptsReturn="True" TextWrapping="Wrap" />
		</StackPanel>
	</StackPanel>
</ContentDialog>
