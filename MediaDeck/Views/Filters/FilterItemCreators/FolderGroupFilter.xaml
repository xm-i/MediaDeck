<local:FolderGroupFilterPageBase x:Class="MediaDeck.Views.Filters.FilterItemCreators.FolderGroupFilter"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:MediaDeck.Views.Filters.FilterItemCreators"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d" >
	<StackPanel Orientation="Horizontal" >
		<TextBlock Text="フォルダグループ" />
		<ComboBox ItemsSource="{x:Bind ViewModel.SearchTypeList}"
			SelectedItem="{x:Bind ViewModel.SearchType.Value, Mode=TwoWay}" >
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding DisplayString}" />
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ComboBox>
		<TextBlock Text="ファイルを検索する" />
		<Button Command="{x:Bind ViewModel.AddFilterCommand}"
			Content="Add Filter" />
	</StackPanel>
</local:FolderGroupFilterPageBase>