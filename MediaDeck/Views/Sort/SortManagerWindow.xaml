<?xml version="1.0" encoding="utf-8"?>
<Window
	x:Class="MediaDeck.Views.Sort.SortManagerWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:MediaDeck.Views.Sort"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	Title="Sort Manager">
	<Grid Padding="12">
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="12" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<local:SortManagerView Grid.Row="0" DataContext="{x:Bind ViewModel, Mode=OneWay}" />

		<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
			<Button Command="{x:Bind ViewModel.SaveCommand}" Style="{StaticResource AccentButtonStyle}">
				<StackPanel Orientation="Horizontal" Spacing="6">
					<FontIcon Glyph="&#xE74E;" FontSize="12" />
					<TextBlock Text="保存" />
				</StackPanel>
			</Button>
		</StackPanel>
	</Grid>
</Window>
