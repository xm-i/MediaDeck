<?xml version="1.0" encoding="utf-8"?>
<local:DetailSelectorUserControl
	x:Class="MediaDeck.Views.Panes.DetailPanes.DetailSelector"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:MediaDeck.Views.Panes.DetailPanes"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">
	<Grid Visibility="{x:Bind ViewModel.TargetFiles.Value,Mode=OneWay, Converter={StaticResource NullToCollapseConverter}}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<local:PropertiesDetail Grid.Row="0" DataContext="{x:Bind ViewModel}" Margin="0,0,0,15"/>		
		<local:TagsDetail Grid.Row="1" DataContext="{x:Bind ViewModel}"/>
	</Grid>
</local:DetailSelectorUserControl>
