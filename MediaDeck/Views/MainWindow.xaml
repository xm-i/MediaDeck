<?xml version="1.0" encoding="utf-8" ?>
<Window x:Class="MediaDeck.Views.MainWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:MediaDeck.Views"
	xmlns:localControls="using:MediaDeck.Views.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:viewerPanes="using:MediaDeck.Views.Panes.ViewerPanes"
	xmlns:filterPanes="using:MediaDeck.Views.Panes.FilterPanes"
	xmlns:detailPanes="using:MediaDeck.Views.Panes.DetailPanes"
	xmlns:repositoryPanes="using:MediaDeck.Views.Panes.RepositoryPanes"
	mc:Ignorable="d"
	Title="MediaDeck" >
	<Grid Loaded="Window_Loaded" >
		<Grid.RowDefinitions>
			<RowDefinition Height="auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<local:NavigationMenu Grid.Row="0"
			DataContext="{x:Bind _viewModel.NavigationMenuViewModel}" />
		<Grid Grid.Row="1" >
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="250" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="250" />
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>
			<repositoryPanes:RepositorySelector Grid.Column="0"
				Grid.Row="0"
				DataContext="{x:Bind _viewModel.RepositorySelectorViewModel}" />
			<filterPanes:FilterSelector Grid.Column="0"
				Grid.Row="1"
				DataContext="{x:Bind _viewModel.FilterSelectorViewModel}" />
			<viewerPanes:ViewerSelector Grid.Column="1"
				Grid.Row="0"
				Grid.RowSpan="2"
				DataContext="{x:Bind _viewModel.ViewerSelectorViewModel}" />
			<detailPanes:DetailSelector Grid.Column="2"
				Grid.Row="0"
				Grid.RowSpan="2"
				DataContext="{x:Bind _viewModel.DetailSelectorViewModel}" />

			<!-- GlobalInfoBarをオーバーレイとして配置 -->
			<localControls:GlobalInfoBar Grid.Column="0"
				Grid.ColumnSpan="3"
				Grid.Row="0"
				Grid.RowSpan="2"
				VerticalAlignment="Top"
				HorizontalAlignment="Center"
				Margin="0,10,0,0"
				Canvas.ZIndex="1000" />
		</Grid>
	</Grid>
</Window>