<?xml version="1.0" encoding="utf-8" ?>
<Window x:Class="MediaDeck.Views.Tools.DuplicateDetectorWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:MediaDeck.Views.Tools"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	Title="Duplicate File Detector" >
	<Window.SystemBackdrop>
		<DesktopAcrylicBackdrop/>
	</Window.SystemBackdrop>
	<Grid Background="Transparent"
		Loaded="Window_Loaded" >
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<!-- カスタムタイトルバー -->
		<Grid x:Name="AppTitleBar" Grid.Row="0" Height="32"
			Background="{ThemeResource TitleBarBackgroundBrush}"
			Padding="12,0,0,0"
			VerticalAlignment="Top" >
			<StackPanel Orientation="Horizontal" VerticalAlignment="Center">
				<Image Source="/Assets/Square44x44Logo.scale-200.png" Width="16" Height="16" Margin="4,0,8,0" />
				<TextBlock Text="{x:Bind Title}" FontSize="12" VerticalAlignment="Center" />
			</StackPanel>
		</Grid>

		<local:DuplicateDetectorContent Grid.Row="1"
			ViewModel="{x:Bind ViewModel}" />
	</Grid>
</Window>