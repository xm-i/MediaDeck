<?xml version="1.0" encoding="utf-8" ?>
<local:NavigationMenuUserControl x:Class="MediaDeck.Views.NavigationMenu"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:MediaDeck.Views"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d" >

	<Grid>
		<Grid.Resources>
			<Storyboard x:Name="NotificationPulseStoryboard"
				RepeatBehavior="Forever" >
				<DoubleAnimation Storyboard.TargetName="NotificationIconTransform"
					Storyboard.TargetProperty="ScaleX"
					From="1.0"
					To="1.2"
					Duration="0:0:0.5"
					AutoReverse="True" />
				<DoubleAnimation Storyboard.TargetName="NotificationIconTransform"
					Storyboard.TargetProperty="ScaleY"
					From="1.0"
					To="1.2"
					Duration="0:0:0.5"
					AutoReverse="True" />
			</Storyboard>
		</Grid.Resources>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>

		<MenuBar Grid.Column="0" Style="{StaticResource TransparentMenuBarStyle}">
			<MenuBarItem Title="Tools" >
				<MenuFlyoutItem Text="Tag Manager"
					Click="MenuFlyoutItem_Click"
					Tag="TagManager" />
				<MenuFlyoutItem Text="Folder Manager"
					Click="MenuFlyoutItem_Click"
					Tag="FolderManager" />
				<MenuFlyoutSeparator />
				<MenuFlyoutItem Text="Duplicate Detector"
					Click="MenuFlyoutItem_Click"
					Tag="DuplicateDetector" />
				<MenuFlyoutSeparator />
				<MenuFlyoutItem Text="Config"
					Click="MenuFlyoutItem_Click"
					Tag="Config" />
				<MenuFlyoutSeparator />
				<MenuFlyoutItem Text="Background Tasks"
					Click="MenuFlyoutItem_Click"
					Tag="BackgroundTasks" />
			</MenuBarItem>
			<MenuBarItem Title="BREAK">
				<MenuFlyoutItem Click="MenuFlyoutItem_Click_1"/>
			</MenuBarItem>
		</MenuBar>

		<Button Grid.Column="1"
			VerticalAlignment="Center"
			Margin="8,0"
			Padding="8,4"
			ToolTipService.ToolTip="未処理のファイル変更があります"
			Click="SyncNotificationButton_Click"
			Visibility="{x:Bind ViewModel.HasUnprocessedChanges.Value, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
			Style="{ThemeResource AccentButtonStyle}" >
			<Button.Content>
				<Grid>
					<FontIcon FontFamily="Segoe Fluent Icons"
						Glyph="&#xE89A;" />
				</Grid>
			</Button.Content>
			<Button.RenderTransform>
				<ScaleTransform x:Name="NotificationIconTransform"
					CenterX="16"
					CenterY="16" />
			</Button.RenderTransform>
		</Button>
	</Grid>
</local:NavigationMenuUserControl>